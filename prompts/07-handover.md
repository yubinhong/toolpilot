# Prompt：任务交接与中断恢复

请把当前线程、工作区和计划整理成另一位工程师或新 Codex 会话可以继续的状态，不开启新的实现范围。

1. 读取 `TASK.md`、`PLANS.md`、工作区 diff、测试结果和相关日志。
2. 更新 `TASK.md`：当前状态、已完成、未完成、验证结果、阻塞和剩余风险。
3. 更新 `PLANS.md`：Progress、Discoveries、Decision Log 和下一条可执行步骤。
4. 更新 `AI_CONTEXT.md` 的活动任务、分支、当前重点、阻塞和下一检查点。
5. 范围外发现进入 `TODO.md`；已发布变化才进入 `CHANGELOG.md`。
6. 不提交、不清理工作区、不丢弃改动、不继续实现下一里程碑。

最终输出一份简短交接：目标、当前状态、改过的关键文件、最后成功的验证、失败/未运行的验证、下一条精确命令、需要的权限或决策。

