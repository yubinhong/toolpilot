# Prompt：发布准备与交付检查

目标版本：`{{VERSION}}`
发布范围：`{{TASKS_OR_COMMITS}}`

请先读取 `AGENTS.md`、`AI_CONTEXT.md`、`PROJECT.md`、相关任务/PRD、`TESTING.md`、`SECURITY.md`、`RUNBOOK.md` 和 `CHANGELOG.md`。

本轮默认只做发布准备，不实际部署：

1. 核对发布范围与任务验收，识别遗漏、未提交改动和不兼容变化。
2. 执行或列出发布质量门槛：测试、构建、安全扫描、迁移验证、备份/恢复和烟雾测试。
3. 检查配置、环境变量、功能开关、依赖、数据库迁移、容量和外部服务准备。
4. 更新 `CHANGELOG.md` 的版本、日期和分类；只写已交付变化，并附迁移/弃用说明。
5. 更新 `RUNBOOK.md` 中的部署、发布后验证、回滚触发和回滚步骤。
6. 为每个高风险步骤给出负责人、观测信号和停止条件。
7. 未获明确授权不得部署、推送标签、创建 Release 或执行生产迁移。

最终输出发布清单，分为：Ready、Blocked、Needs approval。若全部就绪，给出严格按顺序执行的发布与回滚命令，但不要自动执行生产操作。

