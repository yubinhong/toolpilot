# Prompt：把模糊需求变成 PRD

需求原文：

> {{PASTE_RAW_REQUIREMENT}}

请先阅读 `AGENTS.md`、`AI_CONTEXT.md`、`PROJECT.md`、现有 `PRD.md`、`ARCHITECTURE.md` 和相关代码/界面。

你的任务是澄清和固化需求，不写实现代码：

1. 识别用户、触发场景、核心问题、目标结果和业务价值。
2. 对照现有系统指出可复用能力、冲突、依赖和边界。
3. 将需求拆成带 ID 的功能需求、非功能需求和 Given/When/Then 验收标准。
4. 明确“不做什么”、失败/空状态、权限、数据、兼容、可访问性、可观测性、发布和回滚要求。
5. 只有缺失信息会显著改变产品行为、安全边界或开发量时才提出问题；其他未知项用最小假设并标记 `ASSUMPTION`。
6. 更新 `PRD.md`；尚不能批准的内容保持 `DRAFT`，开放问题写明 Owner 和截止条件。
7. 不修改业务代码，不创建超出需求的功能。

最终输出：PRD 摘要、关键决策、假设、开放问题、建议拆成的任务列表和推荐的第一项任务。

