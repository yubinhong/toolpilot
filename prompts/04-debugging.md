# Prompt：可复现诊断与最小修复

问题：`{{BUG_OR_INCIDENT_DESCRIPTION}}`

复现步骤/证据：

```text
{{REPRO_STEPS_LOGS_ERROR}}
```

请先读取项目上下文和当前任务文档，然后：

1. 在安全的本地或测试环境复现问题；记录命令、输入、实际结果和期望结果。若无法复现，收集能够区分假设的证据，不要随机改代码。
2. 沿输入 → 边界验证 → 核心逻辑 → 数据/外部依赖 → 输出/状态的路径定位原因。
3. 区分根因、触发条件和表面症状；列出被证据排除的主要假设。
4. 用户只要求诊断时保持只读；若任务明确要求修复，则先添加回归测试，再实施最小修复。
5. 不改变 API 形状、业务规则、依赖版本或数据模型，除非根因要求且已在 `TASK.md` 获批。
6. 重跑原始复现、最小相关测试和必要回归；检查日志中无密钥或敏感数据。
7. 更新 `TASK.md` 的根因、修复、验证、风险与回滚；范围外问题进入 `TODO.md`。

最终输出：根因、证据链、修复内容（若授权）、验证结果、影响范围和残余风险。避免只给“可能是”而无验证步骤的结论。

